# Assets

Dish visuals for the static menu live in `assets/dishes/`.

The app uses relative paths so the same files work locally and on GitHub Pages. If an image is missing or fails to load, the menu card displays a styled fallback automatically.
